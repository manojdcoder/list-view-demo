<ListView items="{{ items }}" itemTap="onItemTap" loaded="onLoaded">
    <ListView.itemTemplate>
        <StackLayout class="list-item">
            <Label text="{{ $value }}" />
        </StackLayout>
    </ListView.itemTemplate>
</ListView>